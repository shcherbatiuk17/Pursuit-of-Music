<section class="section is-medium is-centered">
    <div class="container">
        <div class="columns">
            <div class="column">
                <figure class="image homepage-image">
                </figure>
            </div>
            <div class="column">  
                <div class="login-signup">
                    <form class="box column form login-form">
                        <div class="field">
                            <label class="label">Email</label>
                        </div>
                        <div class="control">
                            <input class="input form-input" type="email" id="email-login" placeholder="email@email.com">
                        </div>
                        <div class="field">
                            <label class="label">Password</label>
                        </div>
                        <div class="control">
                            <input class="input" type="password" placeholder="Password" id="password-login">
                        </div>
                        <div>
                            <button class="button is-warning is-rounded signin-btn">Login</button>
                            <p class="p-2">Already a member?<a href="/signup"> Register</a></p>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</section>

    {{#each songs as |song|}}
    <div class="row mb-2">
        <div class="col-md-8">
        <h4><a>{{song.title}}</a></h4>
    </div>
    <div class="col-md-4"><button class="btn btn-sm btn-danger" data-id="{{project.id}}">DELETE</button>
    </div>
    {{/each}}
    
<script src="/js/login.js"></script>